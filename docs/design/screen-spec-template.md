# 画面仕様テンプレート
各画面ごとにこのテンプレートをコピーし、必要項目を埋めてください。不要な項目は削除して構いません。

## メタ情報
- 画面名: 
- バージョン/更新日: 
- 作成者/レビュー担当: 
- 対応フェーズ: (例: PH1 / PH2)

## 目的と役割
- この画面でユーザーが達成すること:
- 他画面との関係・遷移元/遷移先:

## ユーザーフロー
- 主なシナリオ:
- 前提状態（ログイン/権限/再生状態など）:
- 終了条件/戻り動線:

## レイアウト概要
- ビューポート想定: (例: iPhone 15 Pro, 390x844pt)
- セクション/コンポーネント構成:
  - ヘッダー:
  - メインコンテンツ:
  - フッター/コントロール:
- レイアウト図やワイヤーのリンク（画像/figma 等）:
- 主要要素の寸法表（任意）:

| 要素 | サイズ(pt) | 余白(pt) | 備考 |
| ---- | ----------- | -------- | ---- |
| 例: シーンラベル | 14 / Semibold | 上 24 | 小さく控えめ |

## コンポーネント詳細
- コンポーネント名: 
  - 内容/役割:
  - ステート: (例: normal / playing / disabled / error)
  - 相互作用: (タップ/長押し/スワイプ など)
  - バインドするデータ:
  - アクセシビリティ: ラベル/ヒント/トレイト/フォーカス順

## データ・状態（StateObservationKit / ステートマシン）
- 画面で扱うステートマシンの状態・遷移:
  - 状態一覧: (例: idle / loading / playing / paused / error)
  - 遷移トリガー: (ユーザー操作 / センサー変化 / タイマー)
  - ガード条件・副作用: (API 呼び出し、オーディオ操作など)
- 購読するストア/パス: (例: `PlaybackStore.state`, `ContextStore.scene`)
- 画面で保持するローカル状態（View 内部）:
- エラー/ローディング表示ルール:
- 状態遷移表（任意）:

| 現在 | イベント/条件 | 遷移先 | 副作用 |
| ---- | ------------- | ------ | ------ |
| idle | ユーザーが再生タップ | loading → playing | Audio start |
| playing | MotionProvider 無効化検知 | error | エラートースト表示 |

## アニメーション・モーション
- 遷移アニメーション: 
- 状態変化アニメーション: 
- 速度/イージング/ディレイの目安:

## サウンド挙動（該当する場合）
- 再生/停止トリガー:
- レイヤー表示との同期ルール:
- フィードバック音の有無:

## 空状態・エラー状態
- 空状態の文言/ビジュアル:
- ネットワーク/権限エラー時の表示とリトライ動線:

## 権限・デバイス前提
- 必要な OS/機能: (例: モーション、位置情報、オーディオ)
- 権限未許可時のハンドリング:

## 計測・ログ（任意）
- 取得するイベント/パラメータ:
- 可観測性（メトリクス/トレース）:

## 未決事項・メモ
- オープンな質問:
- 次のアクション:
